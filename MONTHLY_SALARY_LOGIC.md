# 📊 월급제 근로소득 계산 로직 상세 설명

## 🎯 월급제 vs 시급제 핵심 차이

### 월급제 (MONTHLY)
```dart
salaryType == 'MONTHLY' && monthlySalary > 0
```

### 시급제 (HOURLY)
```dart
salaryType == 'HOURLY' || monthlySalary == 0
```

---

## 💰 1단계: 통상시급 계산

### 월급제의 통상시급
```dart
// 📌 공식: 월급 ÷ 월 소정근로시간
// 월 소정근로시간 = 주소정근로시간 × 4.345주 (주휴 포함)

final weeklyHours = monthly.weeklyHours > 0 ? monthly.weeklyHours : 40.0;
final monthlyHours = weeklyHours * 4.345;  // 예: 40 × 4.345 = 173.8시간
hourlyRate = (worker.monthlySalary / monthlyHours).round();

// 예시 1: 월급 3,000,000원, 주 40시간
// → 통상시급 = 3,000,000 ÷ (40 × 4.345) = 3,000,000 ÷ 173.8 = 17,271원

// 예시 2: 월급 2,000,000원, 주 14시간
// → 통상시급 = 2,000,000 ÷ (14 × 4.345) = 2,000,000 ÷ 60.83 = 32,879원
```

### 시급제의 통상시급
```dart
// 입력된 시급을 그대로 사용
hourlyRate = worker.hourlyRate;
```

---

## 💵 2단계: 기본급 계산

### 월급제의 기본급
```dart
// 📌 월급을 그대로 사용 (정상근로시간과 무관!)
baseSalary = worker.monthlySalary;

// 예시: 월급 2,000,000원
// → 기본급 = 2,000,000원 (고정)
// → 정상근로시간 50시간 입력해도 기본급은 변하지 않음!
```

### 시급제의 기본급
```dart
// 📌 시급 × 정상근로시간
baseSalary = (hourlyRate * normalHours).round();

// 예시: 시급 10,000원, 정상근로시간 50시간
// → 기본급 = 10,000 × 50 = 500,000원
```

---

## 🌙 3단계: 연장/야간/휴일수당 (5인 이상 사업장만)

### 월급제 & 시급제 공통
```dart
// 모두 통상시급 기준으로 계산!

// 연장수당 (1.5배)
overtimePay = hourlyRate × overtimeHours × 1.5

// 야간수당 (0.5배)
nightPay = hourlyRate × nightHours × 0.5

// 휴일수당
// - 8시간 이하: 1.5배
// - 8시간 초과: 8시간까지 1.5배 + 초과분 2.0배
if (holidayHours <= 8) {
  holidayPay = hourlyRate × holidayHours × 1.5
} else {
  holidayPay = (hourlyRate × 8 × 1.5) + (hourlyRate × (holidayHours - 8) × 2.0)
}
```

### 예시: 월급 2,000,000원, 주 14시간
```
통상시급 = 32,879원

연장수당 = 32,879 × 5h × 1.5 = 246,592원
야간수당 = 32,879 × 5h × 0.5 = 82,198원
휴일수당 = 32,879 × 5h × 1.5 = 246,592원
```

---

## 🎉 4단계: 주휴수당

### 월급제: **주휴수당 없음** (이미 월급에 포함)
```dart
if (isMonthlyWorker) {
  weeklyHolidayPay = 0;
  weeklyHolidayFormula = '월급에 포함';
}
```

### 시급제: 주 15시간 이상만 지급
```dart
if (!isMonthlyWorker && weeklyHours >= 15 && weekCount > 0) {
  // 📌 공식: 시급 × 1일 소정근로시간(최대 8h) × 개근주수
  final dailyHours = min(weeklyHours / 5, 8.0);
  weeklyHolidayPay = (hourlyRate × dailyHours × weekCount).round();
} else if (weeklyHours < 15) {
  weeklyHolidayPay = 0;
  weeklyHolidayFormula = '주 15시간 미만 (지급 대상 아님)';
}
```

### 예시: 시급 10,000원, 주 40시간, 4주 개근
```
1일 소정근로시간 = min(40 / 5, 8) = 8시간
주휴수당 = 10,000 × 8 × 4 = 320,000원
```

---

## 📈 5단계: 지급총액

### 공통 계산
```dart
totalPayment = baseSalary              // 기본급
            + overtimePay              // 연장수당
            + nightPay                 // 야간수당
            + holidayPay               // 휴일수당
            + weeklyHolidayPay         // 주휴수당 (시급제만)
            + bonus                    // 상여금
            + additionalPay1~3         // 추가수당
            + taxFreeMeal              // 비과세 식대
            + taxFreeCarMaintenance    // 비과세 차량유지비
            + otherTaxFree;            // 기타 비과세
```

---

## 🏥 6단계: 4대보험 기준액

### 과세소득 계산 (4대보험 기준)
```dart
// 📌 비과세 항목은 제외!
taxableIncome = baseSalary
              + overtimePay
              + nightPay
              + holidayPay
              + weeklyHolidayPay
              + bonus
              + taxableAdditionalPay;  // 과세 추가수당만

// 비과세 합계 (4대보험 기준액에서 제외됨)
totalTaxFree = taxFreeMeal
             + taxFreeCarMaintenance
             + otherTaxFree
             + taxFreeAdditionalPay;

// 4대보험 기준액 = 과세소득
insuranceBase = taxableIncome;
```

### 4대보험 계산
```dart
// 국민연금 (4.5%)
nationalPension = insuranceBase × 0.045

// 건강보험 (3.545%)
healthInsurance = insuranceBase × 0.03545

// 장기요양 (건강보험의 12.95%)
longTermCare = healthInsurance × 0.1295

// 고용보험 (0.9%)
employmentInsurance = insuranceBase × 0.009
```

---

## 💸 7단계: 근로소득세 (간이세액표)

### 간이세액표 적용
```dart
// 📌 월 과세소득으로 계산 (비과세 제외)
monthlyTaxableIncome = taxableIncome;

// 간이세액표로 소득세 계산
// - 이미 근로소득공제, 기본공제, 특별소득공제, 연금보험료공제, 근로소득세액공제가 반영된 값!
taxes = IncomeTaxCalculator.calculateIncomeTax(
  monthlyIncome: monthlyTaxableIncome,
  familyCount: worker.taxDependents,      // 공제대상 가족 수
  childrenCount: worker.childrenCount,    // 8-20세 자녀 수
);

// 소득세율 배율 적용 (80%, 100%, 120%)
incomeTax = (taxes[0] × worker.incomeTaxRate / 100.0) ~/ 10 × 10;
localIncomeTax = (taxes[1] × worker.incomeTaxRate / 100.0) ~/ 10 × 10;
```

### 간이세액표 구조 (1,000만원 이하)
```dart
// 월급여액별, 가족수별 세액표
// 예시: 월 3,000,000원, 가족 2명
// → 소득세: 56,850원
// → 지방소득세: 5,685원

final taxData = {
  3000: {1: 74350, 2: 56850, 3: 31940, 4: 26690, ...},
  // ...
};
```

### 1,000만원 초과 시 계산식
```dart
// A = 1,000만원일 때의 해당 가족 수별 세액

if (monthlyIncome <= 14000000) {
  tax = A + (초과액 × 98% × 35%) + 25,000원
} else if (monthlyIncome <= 28000000) {
  tax = A + 1,397,000원 + (초과액 × 98% × 38%)
} else if (monthlyIncome <= 30000000) {
  tax = A + 6,610,600원 + (초과액 × 98% × 40%)
} else if (monthlyIncome <= 45000000) {
  tax = A + 7,394,600원 + (초과액 × 40%)
} else if (monthlyIncome <= 87000000) {
  tax = A + 13,394,600원 + (초과액 × 42%)
} else {
  tax = A + 31,034,600원 + (초과액 × 45%)
}
```

---

## 🧮 8단계: 공제총액 및 실수령액

### 공제총액
```dart
totalDeduction = nationalPension
               + healthInsurance
               + longTermCare
               + employmentInsurance
               + incomeTax
               + localIncomeTax
               + additionalDeduct1~3;  // 추가공제
```

### 실수령액
```dart
netPay = totalPayment - totalDeduction;
```

---

## 📊 전체 계산 흐름 예시

### 입력 데이터
```
[직원 정보]
- 고용형태: 정규직
- 급여형태: 월급제
- 월급여: 2,000,000원
- 주소정근로시간: 14시간
- 4대보험: 전부 가입
- 공제대상 가족: 2명
- 8-20세 자녀: 1명
- 소득세율: 100%

[월별 입력]
- 정상근로시간: 50시간 (월급제는 무시됨!)
- 연장근로: 5시간
- 야간근로: 5시간
- 휴일근로: 5시간
- 개근주수: 4주
- 5인 이상 사업장: ✅
```

### 계산 결과
```
[1단계: 통상시급]
통상시급 = 2,000,000 ÷ (14 × 4.345) = 32,879원

[2단계: 기본급]
기본급 = 2,000,000원 (월급 그대로)

[3단계: 수당 (5인 이상 사업장)]
연장수당 = 32,879 × 5 × 1.5 = 246,592원
야간수당 = 32,879 × 5 × 0.5 = 82,198원
휴일수당 = 32,879 × 5 × 1.5 = 246,592원

[4단계: 주휴수당]
주휴수당 = 0원 (월급제는 월급에 포함)

[5단계: 지급총액]
지급총액 = 2,000,000 + 246,592 + 82,198 + 246,592 = 2,575,382원

[6단계: 4대보험]
과세소득 = 2,575,382원
국민연금 = 2,575,382 × 0.045 = 115,892원
건강보험 = 2,575,382 × 0.03545 = 91,297원
장기요양 = 91,297 × 0.1295 = 11,823원
고용보험 = 2,575,382 × 0.009 = 23,178원

[7단계: 소득세]
월 과세소득 = 2,575,382원
간이세액표 (2,575,382원, 가족 2명):
→ 소득세: 약 32,000원 (선형보간)
→ 자녀공제: -12,500원 (1명)
→ 최종 소득세: 19,500원
→ 지방소득세: 1,950원

[8단계: 공제총액]
공제총액 = 115,892 + 91,297 + 11,823 + 23,178 + 19,500 + 1,950 = 263,640원

[9단계: 실수령액]
실수령액 = 2,575,382 - 263,640 = 2,311,742원
```

---

## ⚠️ 흔한 오해와 주의사항

### 1. 정상근로시간은 월급제에서 무시됨!
```
❌ 잘못된 이해:
"월급 200만원, 정상근로시간 50시간 입력 → 기본급 계산에 사용?"

✅ 올바른 이해:
"월급제는 정상근로시간과 무관하게 월급을 기본급으로 사용"
"정상근로시간(normalHours)은 시급제만 사용!"
```

### 2. 주소정근로시간은 통상시급 계산에만 사용!
```
❌ 잘못된 이해:
"주소정근로시간 14시간 → 기본급이 줄어든다?"

✅ 올바른 이해:
"주소정근로시간은 통상시급 계산에만 사용"
"월급제 기본급 = 월급여 (고정)"
"통상시급 = 월급여 ÷ (주소정근로시간 × 4.345)"
```

### 3. 월급제는 주휴수당이 별도 지급되지 않음!
```
❌ 잘못된 이해:
"월급제도 주휴수당을 따로 계산해야 한다?"

✅ 올바른 이해:
"월급제는 주휴수당이 이미 월급에 포함되어 있음"
"주휴수당은 시급제만 별도 계산!"
```

### 4. weeklyHours가 14시간이어도 주휴수당은 0원 (정상!)
```
✅ 올바른 이해:
"주 소정근로시간 14시간 < 15시간 → 주휴수당 미지급"
"근로기준법: 주 15시간 이상 근무해야 주휴수당 대상"
"월급제는 어차피 월급에 포함되어 있으므로 계산하지 않음"
```

### 5. 통상시급은 자동 계산됨 (월급제)
```
✅ 올바른 이해:
"월급제는 hourlyRate를 따로 입력하지 않음"
"자동 계산: hourlyRate = monthlySalary ÷ (weeklyHours × 4.345)"
"이 통상시급으로 연장/야간/휴일수당 계산"
```

---

## 🔍 로직 검증 체크리스트

월급제 근로자의 급여 계산이 이상할 때 확인할 사항:

- [ ] 1. **월급여 (monthlySalary)가 입력되어 있는가?**
  - 월급제인데 monthlySalary = 0 → 기본급 0원 발생!

- [ ] 2. **주소정근로시간 (weeklyHours)이 올바른가?**
  - 기본값 40시간 vs 실제 입력값 14시간
  - DB에 40시간으로 저장되어 있으면 통상시급이 잘못 계산됨!

- [ ] 3. **5인 이상 사업장 체크가 되어 있는가?**
  - 체크 안 됨 → 연장/야간/휴일수당 0원
  - 체크 됨 → 연장/야간/휴일수당 계산됨

- [ ] 4. **4대보험 가입 여부가 올바른가?**
  - 미가입 설정 → 해당 보험료 0원

- [ ] 5. **공제대상 가족 수가 정확한가?**
  - 가족 수에 따라 소득세 크게 변동
  - 예: 가족 1명 vs 2명 → 소득세 약 2배 차이

- [ ] 6. **비과세 항목이 과세소득에 포함되지 않았는가?**
  - taxFreeMeal, taxFreeCarMaintenance는 4대보험/소득세 기준액에서 제외

- [ ] 7. **Flutter 앱이 최신 버전인가?**
  - `flutter clean` 후 재실행 필요

---

## 📚 관련 파일

- `lib/services/payroll_calculator.dart` - 급여 계산 로직
- `lib/services/income_tax_calculator.dart` - 간이세액표 계산 로직
- `lib/core/models.dart` - WorkerModel, MonthlyData, SalaryResult
- `lib/core/constants.dart` - AppConstants (세율 상수)

---

## 💡 요약

### 월급제 계산 핵심 3가지

1. **기본급 = 월급여 (고정)**
   - 정상근로시간과 무관!

2. **통상시급 = 월급여 ÷ (주소정근로시간 × 4.345)**
   - 연장/야간/휴일수당 계산의 기준

3. **주휴수당 = 0원 (월급에 포함)**
   - 시급제만 별도 계산

### 월급제 vs 시급제 비교표

| 항목 | 월급제 | 시급제 |
|------|--------|--------|
| 기본급 | **월급여 (고정)** | 시급 × 정상근로시간 |
| 통상시급 | 월급여 ÷ (주소정 × 4.345) | 입력된 시급 |
| 주휴수당 | **0원 (월급 포함)** | 시급 × 일소정 × 개근주수 |
| 정상근로시간 | **무시됨** | 기본급 계산에 사용 |
